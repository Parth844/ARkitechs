using UnityEngine;

/// <summary>
/// Simple anchor container for placing runtime-loaded 3D models.
/// Attach this to the ARModelAnchor prefab.
/// </summary>
public class ARModelAnchor : MonoBehaviour
{
    // Nothing required here — this script simply marks
    // the object as a stable anchor for models.
}
